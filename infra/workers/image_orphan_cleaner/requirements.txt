# AWS SDK (boto3 is included in Lambda runtime, but included for local testing)
boto3>=1.34.0

# Development dependencies (for local testing)
pytest>=7.4.0
moto>=4.2.0  # For mocking AWS services in tests
